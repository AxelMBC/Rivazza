# Loaded only when Vite runs in "demo" mode (`vite --mode demo`), via the
# `dev:demo` / `build:demo` scripts. Replays the committed recording instead of
# connecting to the live bridge (see src/lib/demo.ts). Live mode is the default
# build with this flag absent — nothing to unset.
VITE_DEMO_MODE=1
